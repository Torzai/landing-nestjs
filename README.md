# Backend NestJS - Mentoría Angular

## Setup

```bash
npm install
cp .env.example .env
# Edita .env con tu Gmail y MongoDB
npm run start:dev
```

API en http://localhost:3000

## Endpoints

- POST /bookings - Crear reserva
- GET /bookings - Listar
- GET /bookings/:id - Obtener una
