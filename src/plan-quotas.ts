// Sesiones (de 1h) incluidas en cada plan de suscripción.
// El saldo se consume con cada reserva y no se reinicia semanalmente.
export const PLAN_SESSION_CREDITS: Record<string, number> = {
  Popular: 12,
  Intensivo: 40,
};
